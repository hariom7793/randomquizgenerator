from tkinter import Tk

# Tk().withdraw()   # Keep the root window from appearing if you don't want a full GUI
root = Tk()
root.config(bg="#F0F0F0", padx=10, pady=10)
root.geometry("320x150")
root.resizable(width=False, height=False)
root.title('Random Quiz Generator')


